# Runtime dependencies for the Airflow image (see dockerfile).
#
# Only direct imports of the pipeline code under dags/ belong here; pip
# resolves the transitive tree, and Airflow's own constraints come from the
# apache-airflow==<version> pin installed alongside this file.
#
# dbt is deliberately absent: it installs into an isolated venv from
# dbt/requirements.txt so its dependency tree can't clash with Airflow's.
# Local-only tooling under tools/ has its own tools/analysis/requirements.txt
# and never ships in the image (see .dockerignore).

beautifulsoup4==4.14.3   # HTML -> text for job descriptions (data_modification)
boto3==1.42.54           # S3 raw-payload storage (data_loading, scrape_jobs)
psycopg2-binary==2.9.11  # Postgres/Supabase driver (datawarehouse/*)
PyYAML==6.0.3            # companies.yaml parsing (scrape_jobs, sync_companies)
requests==2.32.5         # ATS APIs + Telegram alerting
